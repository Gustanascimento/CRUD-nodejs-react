class Task {
    constructor(id, title, description, due, status, tags) {
            this.id = id;
            this.title = title;
            this.content = content;
            this.due = due;
            this.status = status;
            this.tags = tags;
    }
}

module.exports = Task;